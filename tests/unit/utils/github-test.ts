import github from 'charlesfries/utils/github';
import { module, test } from 'qunit';

module('Unit | Utility | github', function () {
  // TODO: Replace this with your real tests.
  test('it works', function (assert) {
    const result = github();
    assert.ok(result);
  });
});
